source :gemcutter
gem 'sinatra'
gem 'newrelic_rpm', :require => 'new_relic/control'
gem 'sinatra-synchrony', :require => false
gem 'evented_magick', '>=0.1.1'

group :development do
  gem 'rake'
  gem 'shotgun'
end

group :test do
  gem 'rack-test'
  gem 'rspec'
end
